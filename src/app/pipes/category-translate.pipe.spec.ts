import { CategoryTranslatePipe } from './category-translate.pipe';

describe('CategoryTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
